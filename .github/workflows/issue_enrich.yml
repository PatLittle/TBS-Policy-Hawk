name: Issue Enrichment

on:
  issues:
    types: [opened]
  workflow_dispatch:
    inputs:
      issue_number:
        description: "Issue number to enrich"
        required: true
        type: string

permissions:
  contents: write
  issues: write

concurrency:
  group: policy-hawk-content-writes
  cancel-in-progress: false
  queue: max

jobs:
  enrich-issue:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      contains(github.event.issue.labels.*.name, 'policy-update')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt
          pip install markitdown

      - name: Install Playwright browsers
        run: playwright install --with-deps chromium

      - name: Enrich and analyze issue
        id: enrich
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ISSUE_NUMBER: ${{ inputs.issue_number || github.event.issue.number }}
        run: python scripts/enrich_issue.py

      - name: Commit and push enrichment artifacts
        env:
          ISSUE_NUMBER: ${{ inputs.issue_number || github.event.issue.number }}
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add data/ screenshots/ PolicyEvolution*.md
          if ! git diff --staged --quiet; then
            git commit -m "chore: enrich issue #${ISSUE_NUMBER}"
            git push
          fi

      - name: Mark PIN analysis complete
        if: steps.enrich.outputs.pin_autoanalyzed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ inputs.issue_number || github.event.issue.number }}
        run: gh issue edit "$ISSUE_NUMBER" --add-label '🪄📝AutoAnalyzed'
